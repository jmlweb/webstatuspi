# SSH Configuration for Raspberry Pi Deployment
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# SSH Connection
PI_SSH_HOST=webstatuspi.lan        # Hostname or IP of your Pi
PI_SSH_USER=pi                      # SSH username
PI_SSH_PORT=22                      # SSH port (default: 22)

# Deployment Paths
PI_PROJECT_PATH=/opt/webstatuspi    # Where the project is installed on Pi
PI_SERVICE_NAME=webstatuspi         # systemd service name
